{namespace hash5.templates.ui.Settings}

/**
 *
 */
{template .wrapper}
    <div class="settings">
        <h2>{msg desc="settings modal heading"}Settings{/msg}</h2>
        <div class="form-wrapper"></div>
        <button class="btn enabled primary btn-save">{msg desc="settings modal save btn"}Save{/msg}</button>
        <button class="btn enabled primary btn-cancle">{msg desc="settings modal cancle btn"}Cancle{/msg}</button>
    </div>
{/template}
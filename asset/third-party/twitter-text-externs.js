

var twttr = {};
twttr.txt = {};

/**
 * @param {string} text
 * @param {*=} options
 * @return {Array.<{hashtag: string, indices: [number, number]}>}
 */
twttr.txt.extractHashtagsWithIndices = function(text, options){};

/**
 * @param {string} text
 * @param {*=} options
 * @return {Array.<{url: string, indices: [number, number]}>}
 */
twttr.txt.extractUrlsWithIndices = function(text, options){};